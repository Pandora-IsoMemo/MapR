# Reads image list from file and converts it into the needed data frame format

Reads image list from file and converts it into the needed data frame
format

## Usage

``` r
convertImageListToDataFrame(file)
```

## Arguments

- file:

  path to json file

## Value

data frame with relevant elements from json
