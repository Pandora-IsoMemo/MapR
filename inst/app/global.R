# image_list <- read.csv("../app_data/image_list.csv") # outdated (image_list is provided via upload)
# image_list <- convertJsonToDataFrame(file = "inst/app_data/image_list.json") # outdated (image_list is provided via upload)
